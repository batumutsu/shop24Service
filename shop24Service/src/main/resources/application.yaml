spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shop24schema?useSSL=false
    username: root
    password: Rra@123.123

#JPA Settings
  # Log SQL statements
  jpa.show-sql: false
  # Hibernate database Dialect
  jpa.properties.hibernate.dialect: org.hibernate.dialect.MySQLDialect
  # Hibernate ddl auto for generating the database schema
  jpa.hibernate.ddl_auto: create